// Phase 0 placeholder so the workspace typechecks. The MV3 extension (background,
// content autofill, popup, native messaging) is built in Phase 5.
import type { NmEnvelope } from "@sentinel/shared";

export type Envelope = NmEnvelope;
