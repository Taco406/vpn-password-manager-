// Phase 0 placeholder so the workspace typechecks. The real Vite + React app
// (screens, mock bridge, globe, charts) is built in Phases 1–7.
import type { SentinelBridge } from "@sentinel/shared";

export type Bridge = SentinelBridge;
